spring.application.name=core-banking-api

# The system of record. Runs as its own service; the MCP servers call it over HTTP.
server.port=${COREBANK_API_PORT:8090}

logging.level.root=warn
logging.level.inc.kodingkrafters=info
